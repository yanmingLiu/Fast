import 'package:flutter/material.dart';

class GemsPage extends StatelessWidget {
  const GemsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('vip')));
  }
}
